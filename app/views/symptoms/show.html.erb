<div class="notice"> <%= flash[:message] %> </div>

<h1> <%= @symptom.name %> </h1>

<% if admin_logged_in? %>
  <%= link_to 'Delete Symptom', @symptom, method: :delete, data:{confirm: "Are you sure you want to destroy this symptom?"} %>
<% end %>
